#pragma once

#include "pix3.h"

static const auto SANITY_PIX_RED = PIX_COLOR(255, 0, 0);
static const auto SANITY_PIX_GREEN = PIX_COLOR(0, 255, 0);
static const auto SANITY_PIX_BLUE = PIX_COLOR(0, 0, 255);
